<nav class="navbar navbar-expand-md sticky-top shadow-sm" style="background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(12px); border-bottom: 1px solid var(--border-light); z-index: 1030;">
  <div class="container-fluid px-4 d-flex align-items-center justify-content-between">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <i class="fa-solid fa-house-signal" style="color: var(--accent-primary); font-size: 1.5rem;"></i>
      <span style="font-family: var(--font-display); font-size: 1.4rem; font-weight: 700; color: var(--text-main); letter-spacing: -0.5px;">RoomRadar</span>
    </a>

    <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <i class="fa-solid fa-bars" style="color: var(--text-main); font-size: 1.5rem;"></i>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarContent">
      <div class="navbar-nav align-items-center gap-3 mt-2 mt-md-0">
        <a class="nav-link fw-medium px-3 rounded-pill" href="/" style="color: var(--text-main);">Home</a>
        <a class="nav-link fw-medium px-3 rounded-pill" href="/listings" style="color: var(--text-main);">Explore</a>
        <a class="nav-link fw-medium px-3 rounded-pill" href="/listings/new" style="color: var(--text-main);">Become a Host</a>
        
        <div class="nav-item dropdown ms-md-2 mt-2 mt-md-0">
          <button class="btn btn-light rounded-pill d-flex align-items-center gap-2 px-3 py-2 bg-white shadow-sm" style="border: 1px solid var(--border-light);" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid fa-bars" style="font-size: 1rem; color: var(--text-muted)"></i>
            <i class="fa-solid fa-circle-user" style="font-size: 1.5rem; color: var(--text-muted)"></i>
          </button>
          <ul class="dropdown-menu dropdown-menu-end border-0 rounded-4 mt-2 py-2 shadow-sm" style="min-width: 200px; box-shadow: var(--shadow-lg);">
            <% if (!currentUser) { %>
              <li><a class="dropdown-item fw-medium py-2" href="/signup">Sign up</a></li>
              <li><a class="dropdown-item py-2 text-muted" href="/login">Log in</a></li>
            <% } else { %>
              <li><span class="dropdown-item-text text-muted py-2">Welcome, <b><%= currentUser.username %></b></span></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item py-2 text-muted" href="/logout"><i class="fa-solid fa-arrow-right-from-bracket me-2"></i> Log out</a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>